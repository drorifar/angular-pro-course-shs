import { NgNotIf2Directive } from './ng-not-if2.directive';

describe('NgNotIf2Directive', () => {
  it('should create an instance', () => {
    const directive = new NgNotIf2Directive();
    expect(directive).toBeTruthy();
  });
});
